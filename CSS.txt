/* ==========================================
   ESTILOS UNIFICADOS PARA AGUADAS
   ========================================== */
:root {
    --verde-profundo: #1a4d2e;
    --verde-medio: #2d6a4f;
    --verde-claro: #52b788;
    --amarillo-oro: #ffd60a;
    --crema: #fef9e7;
    --negro: #0d1b2a;
}

* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    font-family: 'Crimson Pro', 'Outfit', serif;
    background: var(--crema);
    color: var(--negro);
    line-height: 1.6;
}

/* Ajustes de Grillas para que no se amontonen */
.historia-content, .rutas-grid, .tradiciones-flex, .button-container {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 2rem;
    max-width: 1200px;
    margin: 0 auto;
    padding: 20px;
}

/* Imágenes Responsivas */
img {
    max-width: 100%;
    height: auto !important;
    display: block;
}

/* ==========================================
   MODO CELULAR (Media Query)
   ========================================== */
@media (max-width: 768px) {
    .hero h1 {
        font-size: 3rem !important; /* Achica el título en el cel */
    }

    .section-title {
        font-size: 2.2rem !important;
    }

    /* Forzamos a que todo sea una sola columna en móviles */
    .historia-content, .rutas-grid, .tradiciones-flex, .button-container {
        grid-template-columns: 1fr !important;
        display: flex !important;
        flex-direction: column !important;
    }

    nav {
        padding: 10px !important;
    }

    .nav-links {
        font-size: 0.8rem;
        flex-wrap: wrap;
        justify-content: center;
    }
}